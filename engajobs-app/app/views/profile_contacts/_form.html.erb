<%= simple_form_for [@public_profile, @profile_contact] do |f| %>
    <%= f.input :profile_contact_type, :label => 'Tipo de contato' %>
    <%= f.input :profile_contact_value, :label => 'Contato' %>
    <%= f.submit %>
<% end %>